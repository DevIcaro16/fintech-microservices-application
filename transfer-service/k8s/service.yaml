apiVersion: v1
kind: Service
metadata:
  name: transfer-service
  namespace: fintech
  labels:
    app: transfer-service
spec:
  selector:
    app: transfer-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
